import React from 'react';
import Add from '../../components/Add';

const HomePage = () => {
    return (
        <div>
            <h1>Home</h1>
            <Add/>
        </div>
    );
};

export default HomePage;